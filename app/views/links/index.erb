<h1>Welcome to your bookmark manager!</h1>
<h2><%= session[:email] %></h2>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tags: <% link.tags.each do |a| %>
        <%= a.tag %>
      <% end %>
    </li>
  <% end %>
</ul>
